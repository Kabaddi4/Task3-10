<!DOCTYPE html>
<html>
  <head>
    <title>Bookers2</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="width=devise-width, initial-scale=1, maximum-scale=1">

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <!--ログイン状態に応じて表示させる画面をここで-->
  <body>

    <header>
      <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
          <a class="navbar-brand">
            <span>Bookers</span>
          </a>
          <button class="navbar-togger collapsed" type="button" data-toggle="collapse"
          data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown"
          aria-expanded="false" aria-label=Toggle navigation>
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="navbar-collapse collapse" id="navbarNavDropdown" sytle>
            <ul class="navbar-nav ml-auto">
            <% if user_signed_in? %>
              <li class="fas fa-link nav-link text-light">
                <%= link_to "Home", books_path %>
              <li>
                 <%= link_to "Users", users_path %>
              </li>
              <li>
                <!--devise/registrations#newへ移行-->
                <%= link_to "Books", books_path %>
              </li>
              <li>
                <!--devise/sessions#newへ移行-->
                <%= link_to "Log out", destroy_user_session_path, method: :delete %>
              </li>
            <% else %>
              <li class="fas fa-link nav-link text-light">
                <%= link_to "Home", root_path %>
              </li>
              <li>
                 <%= link_to "About", about_path %>
              </li>
              <li>
                <!--devise/registrations#newへ移行-->
                <%= link_to "Sign up", new_user_registration_path %>
              </li>
              <li>
                <!--devise/sessions#newへ移行-->
                <%= link_to "Log in", new_user_session_path %>
              </li>
            <% end %>
            </ul>
          </div>
        </div>
      </nav>
    </header>

    <main>
    <%= yield %>
    </main>

    <footer>
      <div class="container mt-5">
        <div class="row">
          <div class="mx-auto">
            <p>CopyRight Infratop</p>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>
