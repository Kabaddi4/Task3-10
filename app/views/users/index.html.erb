<%#edit_user_path あと、controllerで未使用のインスタンス変数があってもエラーは出ないらしい。」%>
<ul class="users">
  <%= link_to "edit", edit_user_path(@userId) %>
  <% @users.each do |user| %>
    <li>
      <p>image</p>
      <%= attachment_image_tag user, :profile_image, :fill, 60, 60 %>
      <p>name</p>
      <%= user.name %>
      <%= link_to user_path(user.id) %>
    </li>
  <% end %>
</ul>