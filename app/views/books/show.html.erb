<div class="container">
  <div class="row">
<%= render 'books/list', user: current_user %>
    <div class="col-md-8 offset-md-1">
      <h2>Book detail</h2>
      <table class="table">
        <tbody>
          <tr>
            <td><%= attachment_image_tag @book.user, :profile_image, :fill, 60, 60 %><br>
                <%= @book.user.name %></td>
            <td><%= @book.title %></td>
            <td><%= @book.body %></td>
        <% if @book.user == current_user %>
            <td><%= link_to "Edit", edit_book_path(@book), class:"btn btn-sm btn-success edit_book" %></td>
            <td><%= link_to "delete", book_path(@book), class:"btn btn-sm btn-danger destroy_book", method: :delete %></td>
          </tr>
        </tbody>
        <% end %>
      </table>
    </div>
  </div>
</div>
  <%#destroyメソッド追加%>
<%#book_path%>